--==[ hydra ]==--

#Structure des commandes
hydra [username] [password] [options] [target] [protocol] -V -f

# username -l, liste username -L
# password -p, liste password -P
# protocol = ssh, smb, ftp, mysql, vnc, postgresql, telnet, ...
# -V = Verbose
# -f Stop si l'username est correcte

#Exemple HTTP avec wordlist password
hydra -l [username] -P [password] -V [target] http-get /directory_protected